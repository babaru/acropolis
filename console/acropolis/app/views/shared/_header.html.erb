<div class="row border-bottom">
    <nav class="navbar navbar-static-top" role="navigation" style="margin-bottom: 0">
        <div class="navbar-header">
            <h5><%= t('messages.welcome_to_use') %><%= Acropolis::Settings.application.name %></h5>
        </div>
        <ul class="nav navbar-top-links navbar-right">
            <li class="dropdown">
                <a class="dropdown-toggle profile-element2" data-toggle="dropdown" href="#">
                    <%= image_tag current_user.avatar.url(:thumb), class: 'img-circle' %>
                    <span class="clear"> <span class="block m-t-xs"> <strong class="font-bold"><%= current_user.full_name %></strong>
                         </span> <span class="text-xs block"> <b class="caret"></b></span> </span>
                </a>
                <ul class="dropdown-menu">
                    <li><%= link_to fa_icon('user', text: t('activerecord.attributes.user.profile')), user_path(current_user) %></a></li>
                    <li class="divider"></li>
                    <li><%= link_to( fa_icon('power-off', text: t("buttons.sign_out")), destroy_user_session_path, :method=>'delete') %></li>
                </ul>
            </li>
        </ul>
    </nav>
</div>