<ul class="nav nav-list">

  <% if recent_items(:product).length > 0 %>
  <li class="nav-header"><%= t('models.recent', model: Product.model_name.human) %></li>
  <% recent_items(:product).map do |id, name| %>
  <%= nav_link_to name, product_product_risk_plans_path(product_id: id), {}, { wrapper: :li } %>
  <% end %>
  <li class="divider"></li>
  <% end %>

  <li class="nav-header"><%= Product.model_name.human %></li>
  <%= nav_link_to t('models.list', model: Product.model_name.human), products_path, {}, { wrapper: :li } %>

</ul>