<div class="row">
    <div class="col-lg-12">
        <div class="m-b-md">
            <div class="">
                <%= link_to fa_icon('plus', text: t('activerecord.text.add', model: CapitalAccount.model_name.human)), new_capital_account_path(product_id: @product.id), class: 'btn btn-white btn-sm' %>
            </div>
        </div>
    </div>
</div>

<%= render 'capital_accounts/capital_accounts_grid' %>
