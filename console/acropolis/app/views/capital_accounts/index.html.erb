<% content_for :page_title do %>

<%= t('models.list', model: CapitalAccount.model_name.human) %>

<% end %>

<% content_for :page_header_right_panel do %>

<div class="btn-toolbar">
  <div class="btn-group pull-right">
      <%= link_to(fa_icon('plus-square', text: t('models.add', model: CapitalAccount.model_name.human)), new_capital_account_path, remote: true, class: 'btn btn-primary') %>
  </div>
</div>

<% end %>

<% content_for :anything_else do %>

<!-- Modal -->

<div class="modal fade" id="item-modal" tabindex="-1" role="dialog" aria-labelledby="myModalLabel" aria-hidden="true">
</div><!-- /.modal -->

<% end %>

<div class="ibox no-drop">
  <div class="ibox-content">
    <div id="capital_account-list" class="project-list tooltip-demo">

      <%= render 'capital_accounts_grid' %>

    </div>
  </div>
</div>