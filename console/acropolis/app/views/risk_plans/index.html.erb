<% hide_sidebar = false if local_assigns[:hide_sidebar].nil? %>

<% content_for :plate_title_panel do %>

<%= render partial: 'risk_plans/risk_plan_panel' %>

<% end %>

<% unless hide_sidebar %>

<% content_for :sidebar do %>

<%= render partial: 'risk_plans/risk_plan_sidebar_menu' %>

<% end %>

<% end %>

<% content_for :anything_else do %>

<!-- Modal -->

<div class="modal fade" id="risk-plan-modal" tabindex="-1" role="dialog" aria-labelledby="myModalLabel" aria-hidden="true">
</div><!-- /.modal -->

<% end %>

<div class="page-header">
  <h2><%= t('navigation.page.risk_plan_list') %></h2>
</div>

<div class="btn-toolbar">
  <div class="btn-group pull-right">
    <%= link_to(fa_icon('plus', text: t('models.new', model: RiskPlan.model_name.human)), new_risk_plan_path, remote: true, class: 'btn btn-primary btn-sm') %>
  </div>
</div>

<p></p>

<div id="risk-plan-list">

  <%= render 'risk_plans_grid' %>

</div>