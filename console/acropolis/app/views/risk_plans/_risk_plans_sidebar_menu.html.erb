<ul class="nav nav-list">

  <% if @risk_plan %>

  <li class="nav-header">本方案关联产品</li>
  <% @risk_plan.products.each do |product| %>
  <%= nav_link_to product.name, product_path(product), {}, { wrapper: :li } %>
  <% end %>

  <li class="divider"></li>
  <% end %>

  <% if recent_items(:risk_plan).length > 0 %>
  <li class="nav-header"><%= t('models.recent', model: RiskPlan.model_name.human) %></li>
  <% recent_items(:risk_plan).map do |id, name| %>
  <%= nav_link_to name, risk_plan_path(id: id), {}, { wrapper: :li } %>
  <% end %>
  <li class="divider"></li>
  <% end %>

  <li class="nav-header"><%= RiskPlan.model_name.human %></li>
  <%= nav_link_to t('models.list', model: RiskPlan.model_name.human), risk_plans_path, {}, { wrapper: :li } %>

</ul>