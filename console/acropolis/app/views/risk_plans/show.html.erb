<% content_for :page_title do %>

<%= t('models.show', model: RiskPlan.model_name.human) %>

<% end %>

<% content_for :sidebar do %>

<%= render partial: 'risk_plans_sidebar_menu' %>

<% end %>

<% content_for :anything_else do %>

<!-- Modal -->

<div class="modal fade" id="item-modal" tabindex="-1" role="dialog" aria-labelledby="myModalLabel" aria-hidden="true">
</div><!-- /.modal -->

<% end %>

<!-- Extra Javascripts -->

<%= content_for :js do %>

<script type="text/javascript">

$(document).ready(function() {

  var elems = Array.prototype.slice.call(document.querySelectorAll('.js-switch'));
  elems.forEach(function(html) {
    var element = $(html);
    if (element.data('switchery') != true) {
      var switchery = new Switchery(html);
    }
  });

});

</script>

<% end %>

<div class="ibox">
  <div class="ibox-content">
      <div class="row">
          <div class="col-lg-12">
              <div class="m-b-md">
                  <%= link_to t('models.edit', model: RiskPlan.model_name.human), edit_risk_plan_path(@risk_plan), remote: true, class: 'btn btn-white btn-xs pull-right' %>
                  <h2><%= @risk_plan.name %></h2>
              </div>
              <dl class="dl-horizontal">

              </dl>
          </div>
      </div>
      <div class="row">
          <div class="col-lg-5">
              <dl class="dl-horizontal">
                  <dt><%= t('activerecord.attributes.risk_plan.created_by') %></dt> <dd><%= @risk_plan.created_by.full_name %></dd>
              </dl>
          </div>
          <div class="col-lg-7" id="cluster_info">
              <dl class="dl-horizontal">

                  <dt><%= t('activerecord.attributes.risk_plan.updated_at') %></dt> <dd><%= @risk_plan.updated_at.strftime('%Y-%m-%d %H:%M:%S') %></dd>
                  <dt><%= t('activerecord.attributes.risk_plan.created_at') %></dt> <dd><%= @risk_plan.updated_at.strftime('%Y-%m-%d %H:%M:%S') %></dd>
              </dl>
          </div>
      </div>
      <div class="row m-t-sm">
          <div class="col-lg-12">
          <div class="panel blank-panel">
          <div class="panel-heading">
              <div class="panel-options">
                  <ul class="nav nav-tabs">
                      <li class="active"><a href="#tab-1" data-toggle="tab"><%= t('models.list', model: RiskPlanOperation.model_name.human) %></a></li>
                      <li><a href="#tab-2" data-toggle="tab"><%= t('navigation.tab.binding_products') %></a></li>
                      <li class=""><a href="#tab-3" data-toggle="tab">Last activity</a></li>
                  </ul>
              </div>
          </div>

          <div class="panel-body">

          <div class="tab-content">
          <div class="tab-pane active" id="tab-1">

          <div class="btn-toolbar">
            <div class="btn-group pull-right">
            <%= link_to(fa_icon('plus-square', text: t('models.add', model: RiskPlanOperation.model_name.human)), new_risk_plan_risk_plan_operation_path(risk_plan_id: @risk_plan.id), remote: true, class: 'btn btn-primary btn-sm') %>
            </div>
          </div>

            <div id="item-list">
              <%= render 'risk_plan_operations/risk_plan_operations_grid' %>
            </div>

          </div>
          <div class="tab-pane" id="tab-2">

            <%= render partial: 'binding_products_grid' %>

          </div>
          </div>

          </div>

          </div>
          </div>
      </div>
  </div>
</div>